# Stack

- Python 3.11
- FastAPI + Pydantic v2
- SQLAlchemy 2.x async + asyncpg + Alembic
- Redis (refresh token revocation, rate limiting, presence, Celery broker, pub/sub)
- Celery (background tasks)
- slowapi (rate limiting)
- pytest-asyncio + httpx (testing)
- ruff + mypy (linting)
